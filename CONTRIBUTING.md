# Contributing

1. Create a focused branch from `main`.
2. Install backend and frontend dependencies using the README instructions.
3. Run `python -m compileall -q src` and `npm run lint` before opening a pull request.
4. Use conventional commit subjects such as `feat:`, `fix:`, `chore:`, and `docs:`.
5. Keep secrets and generated vector databases out of commits.
